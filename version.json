{
  "version": "2026-09-08.105",
  "date": "2026-09-08",
  "changes": [
    "Fixed a false-alarm bug in the Push to Home verification itself — it was flagging genuinely matching data as a mismatch because Firebase sorts object keys alphabetically while the local draft doesn't, and plain JSON comparison is order-sensitive"
  ]
}

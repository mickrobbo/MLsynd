{
  "version": "2026-09-08.102",
  "date": "2026-09-08",
  "changes": [
    "Found the real bug in Group Multi Entry's save: fetch() doesn't throw on HTTP error responses, only network failures — a write Firebase actually rejected could still show 'Draft saved'. Now checks and surfaces the real error",
    "Video Poker: reverted to independent per-hand decks (5 hands = 5 decks) and independent per-hand Hold pills"
  ]
}
